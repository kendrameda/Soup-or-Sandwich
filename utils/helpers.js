// module.exports = {
// increment_index: handlebars.registerHelper("increment", function(index) {
//     return index + 1;
// }),

// };
module.exports = {
    increment: (index) => {
        return index+1;
    }
}